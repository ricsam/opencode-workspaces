# OpenCode Workspaces Helm repository

This branch is managed by the Helm chart release workflow. Add it with:

```bash
helm repo add opencode-workspaces https://ricsam.github.io/opencode-workspaces
```
