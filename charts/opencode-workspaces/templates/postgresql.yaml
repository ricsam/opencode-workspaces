{{- if .Values.postgresql.enabled }}
{{- $postgresqlName := printf "%s-postgresql" (include "opencode-workspaces.fullname" .) }}
{{- $existingPostgresql := lookup "apps/v1" "StatefulSet" .Release.Namespace $postgresqlName }}
{{- $existingClaimLabels := dict }}
{{- if $existingPostgresql }}
  {{- $existingClaimTemplates := get $existingPostgresql.spec "volumeClaimTemplates" }}
  {{- if $existingClaimTemplates }}
    {{- $existingClaimLabels = default (dict) (get (index $existingClaimTemplates 0).metadata "labels") }}
  {{- end }}
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $postgresqlName }}
  labels:
    {{- include "opencode-workspaces.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: {{ include "opencode-workspaces.name" . }}-postgresql
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: postgresql
      port: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ $postgresqlName }}
  labels:
    {{- include "opencode-workspaces.labels" . | nindent 4 }}
spec:
  serviceName: {{ $postgresqlName }}
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "opencode-workspaces.name" . }}-postgresql
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "opencode-workspaces.name" . }}-postgresql
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      automountServiceAccountToken: false
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: postgresql
          image: {{ .Values.postgresql.image }}
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsNonRoot: true
            runAsUser: 999
            runAsGroup: 999
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          env:
            - name: POSTGRES_DB
              value: {{ .Values.postgresql.auth.database | quote }}
            - name: POSTGRES_USER
              value: {{ .Values.postgresql.auth.username | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (include "opencode-workspaces.secretName" .) .Values.postgresql.auth.existingSecret }}
                  key: postgres-password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - name: postgresql
              containerPort: 5432
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.postgresql.auth.username | quote }}, "-d", {{ .Values.postgresql.auth.database | quote }}]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.postgresql.auth.username | quote }}, "-d", {{ .Values.postgresql.auth.database | quote }}]
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            {{- toYaml .Values.postgresql.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          {{- if $existingClaimLabels }}
          {{- toYaml $existingClaimLabels | nindent 10 }}
          {{- else }}
          app.kubernetes.io/name: {{ include "opencode-workspaces.name" . }}
          app.kubernetes.io/instance: {{ .Release.Name }}
          app.kubernetes.io/managed-by: {{ .Release.Service }}
          {{- end }}
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: {{ .Values.postgresql.persistence.storageClass }}
        resources:
          requests:
            storage: {{ .Values.postgresql.persistence.size }}
{{- end }}
