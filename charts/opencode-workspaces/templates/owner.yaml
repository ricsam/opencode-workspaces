apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "opencode-workspaces.fullname" . }}-owner
  labels:
    {{- include "opencode-workspaces.labels" . | nindent 4 }}
data:
  purpose: Owner anchor for dynamic workspace resources. PVCs deliberately do not reference this object.
